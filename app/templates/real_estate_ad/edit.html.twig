{% extends 'base.html.twig' %}

{% block title %}Modification du bien {{ realEstateAd.title }} !{% endblock %}

{% block body %}
    <h1>Modification du bien {{ realEstateAd.title }} !</h1>

    {{ form_start(form) }}
        {{ form_rest(form) }}
        <button type="submit">Modifier</button>
    {{ form_end(form) }}

    <br><br>
    <a href="{{ path('real_estate_ad_index') }}">Voir les biens</a>
{% endblock %}
