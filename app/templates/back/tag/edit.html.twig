{% extends 'base_back.html.twig' %}

{% block title %}Edit Tag{% endblock %}

{% block body %}
    <h1>Edit Tag</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Editer</button>
    {{ form_end(form) }}

    <a href="{{ path('back_tag_index') }}">back to list</a>

    {{ include('back/tag/_delete_form.html.twig') }}
{% endblock %}
